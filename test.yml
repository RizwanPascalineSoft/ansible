- hosts: vmi1098659.contaboserver.net
  become: true
  become_method: sudo
  tasks:
    - shell: /usr/bin/mysqldump -h localhost -uroot -pg@17m8VK9Dbh26 migrationutility > migrationutility.sql
    - fetch:
       src: /var/lib/awx/projects/migrationutility.sql
       dest: /var/lib/awx/projects/
       flat: true
